PORT=4000
NODE_ENV=development

DB_HOST=localhost
DB_PORT=3306
DB_NAME=catalogue_ci
DB_USER=root
DB_PASSWORD=

JWT_SECRET=changez_moi_par_une_longue_chaine_aleatoire_de_32_caracteres
JWT_EXPIRES_IN=7d

CLIENT_URL=http://localhost:5173
PUBLIC_APP_URL=http://localhost:5173

# Seed — identifiants initiaux (utilises par node seeders/seed.js)
SEED_ADMIN_EMAIL=admin@votre-domaine.com
SEED_ADMIN_PASSWORD=VotreMotDePasse@1
SEED_MERCHANT_EMAIL=
SEED_MERCHANT_PASSWORD=

# SMTP — envoi d'emails (optionnel, affiche en console si absent)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
ADMIN_EMAIL=

# Wave API (optionnel)
WAVE_API_KEY=
WAVE_SIGNING_SECRET=
WAVE_WEBHOOK_SECRET=
WAVE_CURRENCY=XOF

# Sync DB (mettre true uniquement en dev pour ALTER TABLE auto)
DB_SYNC_ALTER=false
